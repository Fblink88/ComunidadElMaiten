/**
 * Barrel Export de Componentes UI
 * 
 * Este archivo sirve como punto de entrada único para todos los componentes
 * del sistema de diseño (shared/ui).
 * 
 * Beneficios:
 * - Importar múltiples componentes en una línea
 * - Ejemplo: import { Button, Input, Card } from "@/shared/ui"
 * - Oculta la estructura interna de la carpeta
 */

export { Button } from "./Button"
export { Input } from "./Input"
export { Card } from "./Card"
export { Badge } from "./Badge"
export { Spinner } from "./Spinner"
export { Modal } from "./Modal"